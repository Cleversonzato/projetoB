<div class="row">
  <div class="col-sm-3"></div>
  <div class="col-sm-6">
    <h5>Login</h5>
    <br>
    <form {{action "logar" on="submit"}}>
      <div>{{erro}}</div>
      <div class="form-group">
        <label for="identification">Usuário</label>
        {{input id="identification" value=identification class="form-control"}}
      </div>
      <div class="form-group">
        <label for="password">Senha</label>
        {{input id="password" type="password" value=password class="form-control"}}
      </div>
      <div>
        <button type="submit" class="btn btn-primary">Entrar</button>
      </div>
    </form>
  </div>
  <div class="col-sm-3"></div>
</div>
